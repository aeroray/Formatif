# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Formatif
# Private license-signing key — never commit
.secrets/
# Bundled FFmpeg sidecar binaries (added at release time)
src-tauri/binaries/
*.tsbuildinfo

# Rust / Tauri build output
src-tauri/target/
# Auto-generated capability schemas (regenerated by `tauri dev`/`tauri build`)
src-tauri/gen/schemas/

# Claude Code local session state (machine-specific, not shared)
.claude/settings.local.json
